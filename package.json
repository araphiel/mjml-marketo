{
  "name": "root",
  "private": false,
  "scripts": {
    "clean": "lerna exec -- rimraf lib",
    "build": "lerna exec -- babel src --out-dir lib --root-mode upward"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  }
}
